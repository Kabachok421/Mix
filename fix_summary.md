Исправил ошибку доступа (Missing or insufficient permissions). 

Проблема была в том, что в правилах безопасности базы данных Firestore не были прописаны разрешения на создание и чтение документов в коллекции `transfers`, которая отвечает за установку прямого соединения (P2P).

Я добавил необходимые правила безопасности и применил их в Firebase. Теперь передача файлов должна работать без ошибок!
